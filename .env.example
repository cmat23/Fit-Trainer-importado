# Google OAuth Configuration
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Instructions for Google OAuth Setup:
# 
# 1. Go to https://console.developers.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google Identity API:
#    - Go to "APIs & Services" > "Library"
#    - Search for "Google Identity" and enable it
# 4. Create OAuth 2.0 credentials:
#    - Go to "APIs & Services" > "Credentials"
#    - Click "Create Credentials" > "OAuth 2.0 Client ID"
#    - Choose "Web application"
#    - Add authorized origins:
#      - http://localhost:5173 (for development)
#      - https://yourdomain.com (for production)
# 5. Copy the Client ID and paste it above
# 6. Rename this file to .env
#
# Note: For development/testing, you can use the demo credentials,
# but for production you MUST use your own Google OAuth credentials.